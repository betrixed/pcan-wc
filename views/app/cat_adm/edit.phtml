<?php
$gem = $this->htmlgem;
    $cat = $m->cat;
    $url = $m->url;
$assets->add('bootstrap');
?>

<style>
     label.col-form-label {
        min-width: 100px;
        text-align: right;
        margin-right:20px;
    }
</style>
<div class='header row'>
    <div class="outerwrap pad10">
        <?= $gem->linkTo(['href' =>$url , 'text' =>"Category List", 'class' => "btn btn-outline-warning"]) ?><span class="btn"><?= $m->title ?></span>
    </div>
</div>
<div class="container pad10 outerwrap">
    
<form action="<?= $url . 'post' ?>" method="post">
    <?php if ( isset($cat->id) && $cat->id > 0 ): ?>
    <div class="form-group row">
        <label class="col-form-label" >Id</label>
        <?= $gem->plainText(['name' => 'id', 'value' => $cat->id, "readonly"]) ?>
    </div>
    <?php endif; ?>
     <div class="form-group row">
        <label class="col-form-label" >Name</label>
        <?= $gem->plainText([ 'name' => 'name', 'value' => $cat->name ]) ?>
   </div>
    <div class="form-group row">
        <label class="col-form-label" >Name Clean</label>
        <?= $gem->plainText([ 'name' => 'name_clean', 'value' => $cat->name_clean ]) ?>
  </div>
   <div class="form-check row">
        <label class="col-form-label" >Enabled</label>
         <?= $gem->checkbox([ 'name' => 'enabled', 'checked' => $cat->enabled ]) ?>
      </div>
    <p>
        <input type="submit" value="Save" />
    </p>
</form>
</div>